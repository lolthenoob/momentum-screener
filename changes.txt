

main.py
- Add vertical per-market tick toggles (US/AU/NZ/SG) with colour-coded checkboxes
- Persist active market selection to screener.ini via config.get/set_active_markets()
- Filter market_tickers in _run_screener to only selected markets
- Disable Run Screener button when no markets are selected
- Fix _switch_to_launcher crash (TclError: window isn't packed) by using
  pack_forget/pack instead of before= reference on unpacked widget
- Maximise window to fullscreen when results table opens
- Restore normal window size and launcher dimensions on New Scan return
- Scale launcher minsize, padding, and button sizes with font size setting
- Replace single Close button in results footer with New Scan + Exit buttons

screener_table.py
- Fix column resize: header drag now live-resizes all data cell frames in that
  column via col_cells registry (col_idx -> list of tk.Frame widgets)
- Also resizes frozen pane container width when a frozen column is dragged
- Clear col_cells registry on each _render to prevent stale widget refs
- Add Exit button that walks widget hierarchy to destroy the root Tk window

momentum_calc.py
- Add name_cache table to SQLite schema (symbol, name, fetched_at)
- Rewrite _fetch_names to check DB cache first; only hits yfinance network
  for tickers missing from cache; stores results back to name_cache
- Subsequent runs load all names instantly from DB with no network calls

config.py
- Add active_markets key to DEFAULTS (default: US,AU,NZ,SG)
- Add get_active_markets() and set_active_markets() helpers"
